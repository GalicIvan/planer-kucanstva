{
    "require": {
        "php": "^8.2",
        "laravel/framework": "^11.0",
        "laravel/sanctum": "^4.0",
        "laravel/tinker": "^2.9"
    },
    "_comment": "This is a reference of the packages this project needs. Run `composer create-project laravel/laravel backend` to get a fresh app, then `composer require laravel/sanctum`, then copy the files from this folder into it (see README)."
}
